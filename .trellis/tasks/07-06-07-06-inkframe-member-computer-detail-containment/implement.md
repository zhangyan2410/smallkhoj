# Implementation Plan

1. Add failing source contract tests for member/computer detail containment.
2. Add roles and containment classes to `frontend/app/members/page.tsx`.
3. Add roles and containment classes to `frontend/app/computers/page.tsx`.
4. Run focused source tests.
5. Run TypeScript, scoped lint, `git diff --check`, task validation, and `./twd`
   blocked/accepted proof.
6. Record evidence and spawn a check worker.
