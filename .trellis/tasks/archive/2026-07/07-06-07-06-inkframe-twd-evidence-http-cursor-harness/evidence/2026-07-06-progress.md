# 2026-07-06 Progress

## Completed This Pass

- Created and activated this Trellis task as a child of
  `07-05-inkframe-product-ui-refactor`.
- Wrote detailed `prd.md`, `design.md`, `implement.md`, `implement.jsonl`, and
  `check.jsonl`.
- Rechecked `./twd` connectivity and Chrome state.
- Added HTTP route-boundary tests for chat read cursor flows.
- Sent the latest Postgres schema fixture addition to a Codex check worker on
  `cr-07-06-inkframe-twd-evidence-http-cursor-harness-postgres`.
- Accepted the check worker's small self-fix: restore pre-existing
  `app.dependency_overrides` instead of clearing all overrides after the
  Postgres HTTP test.
- Added follow-up real Postgres schema tests for DM cursor POST/GET, thread
  cursor POST/GET, and channel/DM scope mismatch no-write behavior.
- Sent the browser-proof task plus Postgres follow-up to Codex check worker on
  `cr-07-06-inkframe-browser-proof-and-postgres-followup`; accepted two
  documentation self-fixes in the new browser proof task plan.
- Added a second follow-up set of real Postgres schema tests for missing
  account session, unjoined active Server, and monotonic channel cursor writes.
- Ran focused and combined backend validation.

## Browser Status

Browser proof remains pending because `./twd` still has no connected tab:

```json
{"ok": true, "tabs": [], "count": 0}
```

Chrome diagnostic says Chrome is installed, the Codex Chrome Extension is
installed/enabled, and native host is correct, but Chrome is not running.

The agent did not launch Chrome because Chrome-control rules require user
permission first.

Evidence:

- `evidence/twd-connectivity.md`

## Backend Status

Added:

```text
backend/tests/test_chat_read_cursors_http.py
backend/tests/test_chat_read_cursors_postgres_http.py
```

The new HTTP tests cover:

- channel cursor POST + GET;
- DM cursor POST + GET;
- thread cursor POST + GET;
- monotonic channel cursor write;
- missing session 401;
- unjoined active server 403;
- active-server/member SQL scoping on GET;
- DM/public channel mismatches in both directions with 400;
- `/api/v1/channels` unread projection using actual newer message rows rather
  than a global sequence gap.
- real temporary Postgres-schema channel cursor persistence and unread
  projection across actual DDL/DML and SQLAlchemy async sessions.
- real temporary Postgres-schema DM cursor persistence.
- real temporary Postgres-schema thread cursor persistence and projection.
- real temporary Postgres-schema channel/DM mismatch rejection without cursor
  writes.
- real temporary Postgres-schema missing-session rejection.
- real temporary Postgres-schema unjoined active Server rejection.
- real temporary Postgres-schema monotonic channel cursor write behavior.

Evidence:

- `evidence/backend-http-harness.md`

## Validation

```text
backend HTTP + Postgres HTTP tests: 17 passed
combined backend cursor/account/Postgres tests: 57 passed
backend compile: pass
git diff --check: pass
```

Main-session verification after the check worker fix:

```bash
cd /Users/code/project/smallkhoj-inkframe-object-ui/backend
rtk env PYTHONPATH=. uv run pytest tests/test_chat_read_cursors_http.py tests/test_chat_read_cursors_postgres_http.py -q
rtk env PYTHONPATH=. uv run pytest tests/test_chat_read_cursors.py tests/test_chat_read_cursors_http.py tests/test_chat_read_cursors_postgres_http.py tests/test_server_account_membership.py -q
rtk env PYTHONPATH=. uv run python -m py_compile routers/public_api.py models/slock.py models/__init__.py models/seed.py services/chat_read_cursors.py services/thread_summary.py
cd /Users/code/project/smallkhoj-inkframe-object-ui
rtk git diff --check
```

Results:

```text
17 passed in 1.56s for focused HTTP + Postgres HTTP files
7 passed in 1.67s for focused Postgres HTTP file
57 passed in 1.48s for combined cursor/account/Postgres suite
backend compile: pass
git diff --check: pass
```

Frontend typecheck/lint/tests were not rerun in this pass because this slice
added backend tests and task evidence only. The previous loop's frontend
validation remains available under
`07-06-inkframe-browser-mobile-backend-hardening`.

## Remaining Work

- Browser/mobile evidence still requires a connected `./twd` tab.
- If the user allows launching Chrome, retry Chrome window open, then rerun
  `./twd --compact tabs` and collect chat/task/mobile evidence.
- The only remaining backend-only matrix edge not duplicated in Postgres is the
  emitted SQL scoping assertion, which is intentionally a fake-session harness
  assertion against generated SQL text.
