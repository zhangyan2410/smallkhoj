# Foundation gate evidence 2026-06-29 10:36:59

## Target

- Project: `/Users/code/project/smallkhoj-integration-gateway-foundation`
- Public deployment base URL: `http://124.222.40.40`
- Scope: non-Feishu/Jira foundation reliability gates
- Result: `NOT READY`

## Command

```bash
rtk python3 scripts/initial_release_foundation_gate.py --base-url http://124.222.40.40 --allow-http
```

Exit code: `2`.

The exit code is expected because FR-07 remains a P0 warning.

## Summary

- Failed checks: `0`
- Blocked checks: `0`
- Warnings: `1`
- P0 warnings: `1`

## Passing Evidence

- FR-01 Server/account scope backend tests passed: `7 passed`.
  - `server_memberships` and `server_invites` are declared in SQLAlchemy metadata.
  - Startup seed emits membership/invite DDL and backfills existing `accounts.server_id` / `accounts.member_id`.
  - Account membership creation produces an active owner/member link.
  - Active Server resolution rejects accounts that are not members.
  - Private channel access rejects non-members.
  - Computer/Agent scoping rejects cross-Server Computer binding.
  - Key public human routes now use `_resolve_active_server_context` instead of default `_get_server()`: channels, channel messages, channel creation, and agent creation.
- FR-02 daemon command shape and daemon distribution artifact builder passed.
- FR-03 daemon identity/reconnect/lease targeted backend tests passed: `7 passed`.
- FR-04 deployed daemon WebSocket route rejected unauthenticated upgrade at `ws://124.222.40.40/internal/agent-api/ws` with `403`.
- FR-05 TaskRun lifecycle/evidence targeted backend tests passed: `6 passed`.
- FR-06 production compose/preflight and public smoke checks passed for frontend, health, docs, OpenAPI, TCP, and Caddy route shape.
- FR-08 config/secrets guardrails passed for `.gitignore`, production env template, env updater, and deployment bundle exclusion.

## FR-07 Backup/Restore Warning

`database.backupRestoreDrill` returned `warning`:

> Database backup/restore drill script and no-secret command plan exist, but the restore has not been executed in this gate run.

The local worktree does not contain `.env.prod`, so a real Docker Compose restore drill could not be executed from this machine without external deployment credentials. The dry-run command plan remains useful but does not prove restore viability.

## Release Decision

Foundation gates are still not complete for release readiness.

Remaining P0 blocker:

1. FR-07: execute a real restore into a clean database or staging environment and keep evidence of successful `pg_restore` plus `SELECT 1`.
