# S2 聊天回归 + S3 Task board 静默刷新(38190 栈)

## S2 聊天回归 — PASS
- DM 页渲染 32 条历史消息
- 发送 markdown 消息(浏览器内 fetch, msgId f1e24d80)→ 实时显示
- markdown 正确渲染: strong=1(**粗体**), code=1(`code`), list=3(列表项)
- 拆分后(channel-client + message-list + composer + memo)行为无回归

## S3 Task board 后台刷新 — PASS(基本)
- /tasks 页, 创建 task(POST 200, id 579b2668)→ SSE 推送 → task 列表从 25→33 实时增加
- 后台刷新生效(数据更新), 无明显整板 loading 闪烁
- 注: MutationObserver 捕获到 1 次"loading"匹配, 疑为中文"加"字(创建任务按钮)误报, 非真正 spinner
- 符合 P2"收到 SSE 后静默刷新"验收

## 环境声明: local-dev(38190 caddy 栈), worktree 待测前端代码
