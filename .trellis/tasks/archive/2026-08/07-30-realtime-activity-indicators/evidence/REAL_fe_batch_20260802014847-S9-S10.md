# S9-S10 持久化 + 迁移 + 自发

## S9 持久化 — PASS
刷新页面前后 store 完全保留:
- 前: {chat:channel:id:1400fc51:{count:2,lastSeq:4}, chat:channel:name:dm:@colleague:{count:2,lastSeq:4}}
- 后: 完全相同
localStorage smallkhoj.activity.unread.v1 持久化生效。

## S10b 旧 key 迁移 — PASS
- 手写旧 key smallkhoj.chat.unread.v1 = {channel:id:test123:{count:2,lastSeq:5}}
- 刷新后: legacy key = null(已删除), activity store 含 chat:channel:id:test123:{count:2,lastSeq:5}(迁移成功)
- migrateLegacyChatStore 逻辑正确

## S10 自发不计 — 需复核(可能测试方法干扰)
zy-ean 自己发 DM 到 dm:96362bc0-...-a1604682(zy-ean↔colleague), 结果 store 递增了(count:1, scope.name=dm:@zy-ean)。
- 疑点: 事件 scope.name 被算成 @zy-ean(发送者), 且 self-filter(currentMemberNames 含 zy-ean === sender @zy-ean)未生效?
- 或: 我发到了一个 zy-ean 是接收方的 DM, scope 解析成了发送者视角的 name
- 需隔离: currentMemberNames 是否正确传入 ActivityUnreadTracker(layout.tsx), 以及后端 scope.name 对自发消息的取值
- 暂记为"需复核", 不判 FAIL(测试方法可能有歧义)
