# 2026-07-06 Progress

## Scope Alignment

This task is part of the `07-05-inkframe-product-ui-refactor` delivery tree, not
a separate visual experiment.

The following previously created Trellis scopes are being carried together:

- `07-05-inkframe-product-ui-refactor`: umbrella product acceptance frame for
  the default Inkframe product UI.
- `07-02-chat-event-unread-indicators`: channel/DM/thread unread event language.
- `07-04-ink-material-card-restore-resource`: material restore/resource
  lifecycle contract.
- `07-06-inkframe-material-runtime-chat-events-optimization`: runtime
  productization and backend-owned read/event cursor direction.
- `07-06-inkframe-runtime-browser-proof-product-polish`: previous proof/polish
  loop whose code-level gap around `/tasks` list/detail material activation was
  fixed and reviewed.
- `07-06-inkframe-browser-mobile-backend-hardening`: current proof/hardening
  loop for `./twd`, mobile evidence, and backend route-flow tests.

The task tree has been updated so the three 07-06 optimization/proof tasks are
children of `07-05-inkframe-product-ui-refactor`.

## Browser Connectivity

Current `./twd` status is diagnosed but not solved:

```bash
rtk ./twd --compact tabs
```

returned:

```json
{"ok": true, "tabs": [], "count": 0}
```

Additional extension-level commands returned `NO_TAB`. A Python WebDriver
bridge process is already listening on `127.0.0.1:18765` and
`127.0.0.1:18766`, but no browser tab/extension client is connected.

No browser/mobile acceptance is claimed for this task yet.

Evidence:

- `evidence/browser-connectivity.md`

## Backend Route-Flow Hardening

`backend/tests/test_chat_read_cursors.py` was strengthened from source-text
style checks toward route-handler-level flow checks.

Added tests call the real route handlers:

- `public_api.update_chat_read_cursor(...)`
- `public_api.get_chat_read_cursors(...)`

Covered route-flow paths:

- channel cursor write with active server/member context;
- DM cursor write with DM scope;
- thread cursor write with active server/member context;
- GET cursor list projection for channel, DM, and thread cursors.

Fake test utilities were extended with:

- `_ExecuteResult.all()`
- `_ExecuteResult.scalars()`
- `_FakeSession.commit()`
- `_JsonRequest`

This proves:

```text
active server context -> route handler -> access check ->
chat_read_cursors service -> DB session add/flush/commit -> serializer payload
```

It is still not a full ASGI authenticated HTTP test harness. That gap remains
explicitly documented.

Evidence:

- `evidence/backend-route-flow.md`

## Current Validation

Frontend:

```bash
cd /Users/code/project/smallkhoj-inkframe-object-ui/frontend
rtk env NODE_PATH=./node_modules npx tsc --noEmit
rtk npm run lint -- --max-warnings=0
rtk env NODE_PATH=./node_modules npx tsx --test test/*.test.ts test/*.test.tsx
```

Result from this continuation pass:

```text
TypeScript: pass
ESLint: pass
Frontend tests: 120 pass / 0 fail
```

Backend:

```bash
cd /Users/code/project/smallkhoj-inkframe-object-ui/backend
rtk env PYTHONPATH=. uv run pytest tests/test_chat_read_cursors.py tests/test_server_account_membership.py -q
rtk env PYTHONPATH=. uv run python -m py_compile routers/public_api.py models/slock.py models/__init__.py models/seed.py services/chat_read_cursors.py services/thread_summary.py
```

Result from this continuation pass:

```text
Backend tests: 40 pass / 0 fail
Backend compile: pass
```

Repository whitespace check:

```bash
cd /Users/code/project/smallkhoj-inkframe-object-ui
rtk git diff --check
```

Result from this continuation pass:

```text
pass
```

## Remaining Gaps

- `./twd` has no connected tab, so real browser evidence is still missing.
- Mobile 390px evidence is still missing.
- Full ASGI authenticated HTTP route-flow tests for read cursors are still
  missing.
- The current backend tests are meaningful route-handler-level coverage, but not
  end-to-end public API authentication coverage.

## Review Request Focus

The next review should check:

- whether the previous Trellis scopes are now correctly consolidated under
  `07-05`;
- whether backend route-handler-level tests are a truthful improvement without
  overstating HTTP coverage;
- whether browser/mobile evidence is represented truthfully as pending;
- whether any code/doc claims imply large ink blob persistence, IndexedDB, or
  backend storage, which remain out of scope.
