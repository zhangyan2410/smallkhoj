# Foundation gate evidence 2026-06-29 10:25:44

## Target

- Project: `/Users/code/project/smallkhoj-integration-gateway-foundation`
- Public deployment base URL: `http://124.222.40.40`
- Scope: non-Feishu/Jira foundation reliability gates
- Result: `NOT READY`

## Command

```bash
rtk python3 scripts/initial_release_foundation_gate.py --base-url http://124.222.40.40 --allow-http
```

Exit code: `2`.

The exit code is expected because the gate treats P0 warnings as not ready.

## Summary

- Failed checks: `0`
- Blocked checks: `0`
- Warnings: `2`
- P0 warnings: `2`

## Passing Evidence

- Tracking: the foundation risk register exists and includes required P0 risks. This is now reported as `FOUNDATION`, not as FR-01 proof.
- FR-02 daemon command shape and daemon distribution artifact builder passed.
- FR-03 daemon identity/reconnect/lease targeted backend tests passed: `7 passed`.
- FR-04 deployed daemon WebSocket route rejected unauthenticated upgrade at `ws://124.222.40.40/internal/agent-api/ws` with `403`.
- FR-05 TaskRun lifecycle/evidence targeted backend tests passed: `6 passed`.
- FR-06 production compose/preflight and public smoke checks passed for frontend, health, docs, OpenAPI, TCP, and Caddy route shape.
- FR-08 config/secrets guardrails passed for `.gitignore`, production env template, env updater, and deployment bundle exclusion.

## FR-01 Server/Account Scope Warning

`server.accountScopeBackendTests` returned `warning`:

> Server/account/channel isolation is tracked but does not yet have dedicated backend proof.

The expected proof file is:

```text
backend/tests/test_server_account_membership.py
```

Required evidence remains:

1. Account owns or joins a Server through membership.
2. Active Server is validated against account membership.
3. Private channel read/write rejects non-members.
4. Computer and Agent creation are scoped to the selected Server.

This warning is intentionally separate from `foundation.riskRegister`; mentioning FR-01 in the risk register does not prove account/server isolation.

## FR-07 Backup/Restore Warning

`database.backupRestoreDrill` returned `warning`:

> Database backup/restore drill script and no-secret command plan exist, but the restore has not been executed in this gate run.

The dry-run plan still includes backup, restore DB creation, restore, verification, and cleanup, but it does not prove the database can be restored.

## Release Decision

Foundation gates are not complete for release readiness.

The remaining P0 blockers are:

1. FR-01: implement/run dedicated Server/account/channel isolation tests.
2. FR-07: execute a real restore into a clean database or staging environment and keep evidence of successful `pg_restore` plus `SELECT 1`.
