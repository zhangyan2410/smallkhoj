# FOUNDATION_gate_20260629100145

## Commands

```bash
rtk .venv/bin/python -m pytest tests/test_daemon_command_generation.py -q
```

```bash
rtk python3 scripts/build_daemon_distribution.py --output-dir /tmp/smallkhoj-daemon-artifacts --platform darwin-arm64 --json
```

```bash
rtk python3 scripts/initial_release_foundation_gate.py --base-url http://124.222.40.40 --allow-http --json
```

## Result

- Overall foundation gate: not ready
- Failures: 0
- Blocked: 3
- Warnings: 0

## FR-02 Evidence

The product daemon command no longer uses the source checkout launcher path:

- backend command test passed: `2 passed`
- `daemon.commandShape`: passed
- command shape is now:
  - `smallkhoj-daemon connect --token <ticket> --server <server>`
  - `smallkhoj-daemon start --machine-token <token> --server <server>`

Versioned daemon distribution builder exists and produced a macOS arm64 artifact:

- artifact: `/private/tmp/smallkhoj-daemon-artifacts/smallkhoj-daemon-v0.2.0-darwin-arm64.tar.gz`
- checksum: `/private/tmp/smallkhoj-daemon-artifacts/smallkhoj-daemon-v0.2.0-darwin-arm64.tar.gz.sha256`
- manifest: `/private/tmp/smallkhoj-daemon-artifacts/smallkhoj-daemon-v0.2.0-darwin-arm64.tar.gz.manifest.json`
- sha256: `719721d220d36f1c8cd50b0cb576b26ff800d20e9039cd3bb52617ac7245df3a`

## Current P0 Status

- `FR-01`: passed only for risk-register coverage. Real Server/account isolation tests remain required.
- `FR-02`: passed for command shape and versioned artifact builder.
- `FR-03`: blocked. No executable duplicate Computer identity/reconnect gate wired yet.
- `FR-04`: passed. Public daemon WebSocket route rejects unauthenticated upgrade with `403`.
- `FR-05`: blocked. No executable TaskRun lifecycle/evidence gate wired yet.
- `FR-06`: passed. Production repo preflight and public smoke pass against `http://124.222.40.40`.
- `FR-07`: blocked. No executable backup/restore drill wired yet.
- `FR-08`: passed. Static config/secrets guardrails pass.

## Remaining Caveat

The artifact is versioned and no longer depends on a source checkout, but it still requires a Node runtime on the target machine. A native self-contained binary, signing/notarization, and auto-update are later hardening items unless real users hit install friction.
