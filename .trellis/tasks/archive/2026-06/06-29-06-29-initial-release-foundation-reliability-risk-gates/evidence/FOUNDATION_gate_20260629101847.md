# Foundation gate evidence 2026-06-29 10:18:47

## Target

- Project: `/Users/code/project/smallkhoj-integration-gateway-foundation`
- Public deployment base URL: `http://124.222.40.40`
- Scope: non-Feishu/Jira foundation reliability gates
- Result: `NOT READY`

## Command

```bash
rtk python3 scripts/initial_release_foundation_gate.py --base-url http://124.222.40.40 --allow-http
```

Exit code: `2`.

The exit code is expected for this run because the gate now treats P0 warnings as not ready.

## Summary

- Failed checks: `0`
- Blocked checks: `0`
- Warnings: `1`
- P0 warnings: `1`

## Passing Evidence

- FR-02 daemon command shape and daemon distribution artifact builder passed.
- FR-03 daemon identity/reconnect/lease targeted backend tests passed: `7 passed`.
- FR-04 deployed daemon WebSocket route rejected unauthenticated upgrade at `ws://124.222.40.40/internal/agent-api/ws` with `403`.
- FR-05 TaskRun lifecycle/evidence targeted backend tests passed: `6 passed`.
- FR-06 production compose/preflight and public smoke checks passed for frontend, health, docs, OpenAPI, TCP, and Caddy route shape.
- FR-08 config/secrets guardrails passed for `.gitignore`, production env template, env updater, and deployment bundle exclusion.

## FR-07 Backup/Restore Warning

`database.backupRestoreDrill` returned `warning`:

> Database backup/restore drill script and no-secret command plan exist, but the restore has not been executed in this gate run.

Dry-run script:

```bash
rtk python3 scripts/postgres_backup_restore_drill.py --dry-run --json
```

Dry-run planned these steps without printing secret values:

1. `backup`
2. `drop-restore-db-before`
3. `create-restore-db`
4. `restore`
5. `verify-restore`
6. `drop-restore-db-after`

The dry-run verifies the command shape only. It does not prove the production database can be restored.

## Release Decision

Foundation gates are not complete for release readiness.

FR-07 remains a P0 warning and should be treated as a release blocker unless the user explicitly narrows the release definition. The next validation must run a real restore into a clean database or staging environment and keep evidence of the successful `pg_restore` plus `SELECT 1` verification.

FR-01 also remains only partially covered: the current runner proves the P0 risk is tracked, not that Server/account/private-channel isolation works. That implementation and proof remain in `06-29-06-29-initial-release-server-account-membership-foundation`.
