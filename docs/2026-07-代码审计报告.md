# SmallKhoj 2026-07 代码审计报告

> **状态：独立复核完成，修复尚未进入 `main`。**
>
> 本文在 2026-07-23 根据真实源码、advisor 分支、聚焦测试、组合冲突和一次性
> PostgreSQL 16 探针重新校正。旧版把“advisor 分支写过代码”直接表述为“当前仓库已
> 修复、全部 APPROVED”，该表述不成立。

## 一句话结论

原审计发现的大多数问题属实，但原有修复只有少数可以直接批准。25 个计划中，独立复核
得到：`APPROVE` 3 项、`APPROVE WITH DEPENDENCY` 5 项、`REVISE` 12 项、
`NOT IMPLEMENTED` 4 项、`SUPERSEDED` 1 项。

其中至少两项已经用真实 PostgreSQL 证明是部署/事务阻断，而不是理论风险：

1. `messages.seq` identity 迁移没有把序列推进到历史最大值之后，升级后第一条自动消息
   可能从 1 开始并撞唯一键。
2. Task DELETE 先删除 Task，再写仍引用旧 Task UUID 的 ActivityLog/EventRecord，外键会
   使整笔事务回滚，Task 实际没有删除。

当前正确状态不是“等合并部署”，而是“独立复核完成，进入重新修复与组合验证阶段”。

## 审计口径

每项结论均按以下证据链判断：

```text
原始 finding → plan 契约 → 实际 commit/diff → 测试/运行证据 → 组合影响 → verdict
```

必须区分：

1. finding 在审计基线是否属实；
2. advisor 分支是否正确修复；
3. 修复是否进入当前 `main`；
4. 所有修复组合后是否可部署。

advisor 分支测试通过只能证明该分支中的现有断言通过，不能自动证明断言覆盖了真实契约，
也不能证明代码已进入 `main` 或可与其他分支组合。

## 总体 verdict

| Plan | 独立 verdict | 核心结论 |
|---|---|---|
| 001 | `APPROVE` | 默认 pytest 基线真实恢复，321 passed |
| 002 | `REVISE` | public key 部署链断裂、权限未真正默认拒绝、credential 仍进 URL |
| 003 | `SUPERSEDED` | 已拆成 003a/003b；不代表 003b 正确 |
| 003a | `APPROVE` | scheduler 日志与退避正确，完整分支 324 passed |
| 003b | `REVISE` | 依赖的 identity 迁移未对齐历史 seq |
| 004 | `REVISE` | 旧库接入、stamp、sequence、启动 create-all 都有部署阻断 |
| 005 | `REVISE` | reply-count N+1 仍在，计划查询预算未达到 |
| 006 | `APPROVE WITH DEPENDENCY` | 文档方向大体正确，需 maintainer 批准并修正过度概括 |
| 007 | `NOT IMPLEMENTED` | standalone observer 存在/WIP，但没有主应用集成 |
| 008 | `NOT IMPLEMENTED` | Work Item/DispatchAttempt 是后续产品能力，尚无设计实现闭环 |
| 009 | `REVISE` | Task DELETE 在真实 PostgreSQL 上触发外键错误并回滚 |
| 010 | `NOT IMPLEMENTED` | `/daemon` 与 `/control/*` 的产品方向未选择、未实现 |
| 011 | `NOT IMPLEMENTED` | Remotion PRD/WIP 存在，仓库没有可验收 scaffold |
| 012 | `REVISE` | 阻止 impersonation，但破坏合法 `@handle`/UUID 自引用 |
| 013 | `REVISE` | 并发首次注册仍可能产生两个 owner |
| 014 | `REVISE` | 普通用户可创建全局 builtin，legacy/slug 迁移与租户语义错误 |
| 015 | `APPROVE WITH DEPENDENCY` | 应用内存读取 cap 有效；ingress/临时磁盘 DoS 未闭环 |
| 016 | `APPROVE WITH DEPENDENCY` | NOTIFY 连接复用有效；恢复策略与连接预算缺失 |
| 017 | `REVISE` | agent SSE 仍持有 request-scoped DB session |
| 018 | `REVISE` | 跨 channel 游标、filter-before-limit、前端翻页均错误或缺失 |
| 019 | `REVISE` | Ruff 73 errors，frontend build 缺生产必需 env，CI 必红 |
| 020 | `APPROVE` | 正确保留真实使用的 `ws`，Bun 单 lockfile 门禁通过 |
| 021 | `APPROVE WITH DEPENDENCY` | code split/边界可构建，缺真实 UI 验收且旧报告措辞过度 |
| 022 | `APPROVE WITH DEPENDENCY` | task refresh 已 targeted；依赖 017 且页面仍可能双 SSE |
| 023 | `REVISE` | e2e 没有有效登录/active-server setup，不能称 canonical |

## 已确认的高风险真实缺陷

### 1. Alembic 与 `messages.seq` 仍不可安全部署

原始 finding 属实：应用曾使用 `max(seq)+1`，并发时会分配相同序号。把生成责任移到数据库
identity 的方向正确，但 advisor 迁移没有处理历史数据和过渡期 writer。

真实 PostgreSQL 16 探针执行：

```text
upgrade baseline
→ 显式插入 messages.seq=1,2,3
→ upgrade 0002_messages_seq
→ 省略 seq 插入下一条消息
```

结果：identity sequence 仍从 1 开始，第一条隐式插入报：

```text
duplicate key value violates unique constraint "messages_seq_key"
Key (seq)=(1) already exists
```

此外：

- `docs/migration-workflow.md` 的旧方案建议 `alembic stamp head`，会把未执行迁移标为已执行；
- 旧库已有表但没有 `alembic_version` 时，直接从 0001 upgrade 会撞已有表；
- FastAPI lifespan 仍调用 `create_tables()` / `Base.metadata.create_all`，会制造“schema 看似最新、
  迁移版本未知”的第三种状态。

正确终态应是：只读 legacy fingerprint/preflight → 只 stamp 已证明等价的 baseline revision →
upgrade head；迁移把 identity restart 到所有历史/显式值之上；应用启动不再执行隐式 DDL。

### 2. Task DELETE 真实事务会失败

advisor 009 的 fake-session 测试通过，但没有执行 PostgreSQL 外键。路由顺序是：

```text
DELETE tasks
→ INSERT activity_logs(task_id=刚删除的 UUID)
→ INSERT event_records(task_id=刚删除的 UUID)
```

真实 PostgreSQL 分别稳定命中：

```text
activity_logs_task_id_fkey
event_records_task_id_fkey
```

事务回滚后 Task 仍存在，activity/event 均为 0。`ON DELETE SET NULL` 只处理删除时已经存在
的引用，不允许删除后新插入一个指向不存在实体的 FK。

正确终态应先捕获 primitive tombstone，再删除依赖/实体，然后用 `task_id=NULL` 记录活动和
专用 `task.deleted` 事件，把旧 UUID 放在 details/payload 中，commit 后再 publish。

### 3. 鉴权和租户隔离未闭环

- 后端读取 `PUBLIC_API_KEY`，生产 compose/env/前端主要使用 `NEXT_PUBLIC_API_KEY`；自定义
  key 会导致前后端认证断裂。
- 默认 key 仍是仓库已知值；生产未配置不能安全回落。
- credential 仍通过 `?api_key=` 进入 URL，可能出现在代理日志、历史和诊断记录。
- 所谓 permission “default-deny”仍默认允许当前代码检查的全部权限，只拒绝未来未知字符串。
- actor 修复只接受裸 display name，合法 `@alice` 和 viewer UUID 被误判 403。
- first-user-wins 没有数据库并发约束，两个独立事务可各自观察零 membership 并提交两个 owner。
- 普通用户可创建 `server_id=NULL` 的全局 builtin；legacy NULL 行会被查询隐藏；slug 仍全局
  唯一，造成跨租户干扰和枚举风险。

这些问题必须作为一个跨 backend/frontend/compose/数据库的终端合同修复，不能只补单点测试。

### 4. 查询、分页和长连接仍有正确性问题

- `/search`、`/history` 中 reply count 仍可能每条消息单独查询；005 没有达到自己的 query-
  count 验收。
- 用 `None` 同时表达“已经预取且不存在”和“没有提供预取值”，会触发意外回查。
- agent `/events` 仍声明 `Depends(get_db)`；FastAPI yield dependency 会在 StreamingResponse
  完成后才退出，因此长流仍占住 session/事务。
- NOTIFY pool acquire/execute 失败后只记录并返回，没有恢复/重建策略。
- server-wide task 游标只用 `task_number`，但唯一键是 `(channel_id, task_number)`；跨 channel
  会重复或漏数据。
- `/threads` 先 limit roots、再在 Python 过滤无回复 root，可能返回空页，即使更老数据仍有
  合格 thread。
- 多个前端页面只请求第一页，不消费 `nextCursor`，第 51 条以前的数据会从 UI 消失。
- 页面可能挂载两个 `RealtimeRefresh`，形成两条 SSE；task targeted refresh 不等于所有页面
  refresh 已被消除。

### 5. CI 与 e2e 不能作为发布门禁

019 中新增脚本本身可运行，但按提交的 workflow：

- `uv run ruff check .` 当前报告 73 个错误；
- `bun run build` 没有 Better Auth/bridge/public-key 所需的非敏感 CI env，会在 production build
  阶段失败。

023 的管理流程没有登录或注入有效 session，也没有 active server/account context；受保护页面
会跳 `/login`。测试还硬编码 `sk_public_local`。因此不能直接把它声明为 canonical e2e。

仓库可见 UI 验收必须使用项目 `./twd` wrapper，并在隔离 worktree runtime 上记录 CWD、分支、
URL、DOM/network/trace markers。已有 advisor 分支没有这些证据。

## 可以保留或复用的工作

以下实现有直接证据，可作为 remediation 的候选来源，但仍需在集成候选上重新跑完整门禁：

- 001：pytest 默认收集与 uv dependency group 配置；
- 003a：scheduler 异常日志、指数退避和成功 reset；
- 020：删除零 importer 的 websocket hook/依赖、保留 `frontend/server.ts` 使用的 `ws`、统一 Bun；
- 015：应用层 `_read_capped` 可作为 defense-in-depth；
- 016：NOTIFY 使用 pool 的主方向；
- 021：markdown/dnd-kit dynamic import、loading/error 边界；
- 022：task event targeted invalidation 的局部设计。

advisor 分支是“不可信 patch source”：复用前必须在 remediation base 重现 intended RED，检查
测试是否覆盖真实合同，再选择 cherry-pick 自包含提交或重新实现。

## 未单独成 plan 的 finding

- TDA-01：`public_api.py` / `agent_api.py` 是多千行巨型路由，属实且未解决；
- TDA-03：Feishu `_nested`、outcome helpers 有重复，属实但优先级低；
- TDA-05 / FRONTEND-01：`channel-client.tsx` 约两千行且职责集中，属实；
- TDA-06：public/agent serializer shape 与 helper 尚未收敛；
- FRONTEND-05：channel client 与 `chat-data-context` 是两套状态所有者；
- TEST-02：鉴权边界还没有完整 contract suite；
- DEP-01：目前更准确是依赖/ADR 边界风险，不是已证明可利用漏洞；
- DOCS-01：frontend README 仍推荐 npm/yarn/pnpm，与 Bun truth 冲突；
- DOCS-02：测试工具说明有矛盾，但正确方向是先修 e2e auth，再同步规则；
- TDA-02 部分误报：markdown/dnd-kit 是有效 code-split 目标；landing dicebear 在 Server
  Component 生成 data URI，并未进入 client 首屏 bundle。

这些维护风险会在功能修复完成后，先补 characterization tests，再逐域拆分；不会用一次大重写
掩盖行为变化。

## 产品方向项的正确分类

- 006：建议接受已上线的 water/dark/shuimo 三主题为产品真相，校正 `DESIGN.md` 并归档旧
  handoff；需 maintainer 明确批准。
- 007：standalone session observer 是独立产品/WIP；主应用集成应另立任务，当前建议延期链接。
- 008：durable Work Item/DispatchAttempt 是有价值的新能力，不是现有回归缺陷；建议链接
  capability matrix 后另立 feature program。
- 009：删除事务是确定缺陷，必须修复，不能按产品方向延期。
- 010：建议把 `/daemon` 迁到 `/control/daemon`，保留一版临时 redirect；需 maintainer 决策。
- 011：Remotion 任务/目录属于用户 WIP；未授权不得删除或补 scaffold，建议显式延期链接。

## 当前仓库与报告状态

截至本次校正前：

- advisor 001–023 的实现均未进入当前 `main`；
- 旧的 merge 模拟只合到部分分支，并停在 005/018 的真实冲突，不能代表完整组合；
- 当前 `DESIGN.md` 仍是旧版，plan 006 的重写只存在于 advisor 分支；
- `docs/migration-workflow.md` 在当前 `main` 尚不存在，不能按旧报告说法视为已发布文档；
- 两份旧报告和原 plan index 的 “DONE / APPROVED” 是执行者自述，不是独立发布 verdict。

## 修复 program

独立复核后已建立 `.trellis/tasks/07-22-codebase-audit-remediation/`，按依赖拆成七个子任务：

1. schema integrity：Alembic、identity、legacy adoption、删除 tombstone；
2. auth/tenancy：credential transport、permissions、actor、owner race、template scope；
3. runtime/querying：查询预算、上传资源、NOTIFY/SSE、分页、实时连接所有权；
4. delivery/UI：CI、Bun、e2e auth、删除/loading/error/targeted refresh、`./twd`；
5. architecture debt：characterization 后拆路由/client/helper/state owner；
6. product directions：006–011 的明确批准、延期、拒绝或链接；
7. integration release：真实 PostgreSQL、完整 backend/frontend、runtime、`./twd`、重新审计。

修复顺序是 schema → auth/tenancy → runtime/querying → delivery/UI → architecture → product
dispositions → 一个完整集成候选。任何 child 分支的单独绿灯都不能替代最终组合验证。

## 完成标准

只有同时满足以下条件，报告才可以改为“已修复”：

- 每个真实缺陷都有 intended-reason RED、修复后的 GREEN 和完整回归；
- migration/并发/FK 使用隔离真实 PostgreSQL，不用 create-all/fake session 代替；
- public/agent SSE 通过真实 ASGI dependency-finalizer 生命周期测试；
- backend pytest/Ruff 与 frontend frozen Bun/tests/lint/typecheck/build 全绿；
- CI 从干净 checkout 在非敏感 env 下可重复通过；
- worktree-specific runtime 用 `smallkhoj-trace` 与 `./twd` 完成可见流程证据；
- 每个原始 finding 在同一个集成 candidate SHA 上重新判定；
- PR 审查和项目 squash merge 实际完成后，才可写“已进入 main”；
- 部署与健康观察实际完成后，才可写“已发布/健康”。

## 关联证据

- 完整技术审计：`.trellis/tasks/07-20-07-19-codebase-audit/research/2026-07-19-codebase-audit.md`
- 独立复核：`.trellis/tasks/07-20-07-19-codebase-audit/research/2026-07-22-independent-verification.md`
- 原执行计划：`.trellis/tasks/07-20-07-19-codebase-audit/plans/`
- 修复父任务：`.trellis/tasks/07-22-codebase-audit-remediation/`
- 七个修复子任务：`.trellis/tasks/07-22-audit-remediation-*/`

本文是“独立复核后的当前真相”，不是修复完成声明。后续集成 release 必须在实际证据变化后再次
更新本文。
