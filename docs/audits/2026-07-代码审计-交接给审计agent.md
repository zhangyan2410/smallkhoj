# 2026-07 代码审计：交接给后续审计 / 修复 agent

> **权威状态（2026-07-23）：独立复核已完成，修复尚未进入 `main`。**
>
> 旧交接把 advisor 分支的执行者自述当成发布事实。后续 agent 不得继承任何 `DONE`、
> `APPROVED` 或“测试通过”声明；必须从同一个集成 candidate 的实际证据重新判断。

## 1. 任务目标

后续工作不是简单合并 20 个 advisor 分支，而是：

```text
确认真实缺陷
→ 在隔离 remediation worktree 中写 intended-reason RED
→ 实现终端合同
→ 真实 PostgreSQL / ASGI / frontend / runtime / ./twd 验证
→ 在单一 candidate SHA 上重新审计
→ 形成可审查合入结果
```

完整独立复核位于：

```text
.trellis/tasks/07-20-07-19-codebase-audit/research/
  2026-07-22-independent-verification.md
```

该文档优先于旧报告、plans/README 状态和 advisor 执行者总结。

## 2. 证据优先级

按以下顺序信任证据：

1. 当前 remediation candidate 的源码、迁移和配置；
2. 在该 candidate 上刚运行的真实数据库/ASGI/full-suite/runtime/UI 证据；
3. 独立复核中的真实 PostgreSQL RED 与静态控制流证明；
4. 原始审计 finding 与 plan 契约；
5. advisor branch diff/测试，仅作为候选 patch 线索；
6. 执行者自述、旧 `DONE` 状态和合并预测，不可作为完成证据。

每项必须区分：finding 是否属实、advisor 是否修对、是否进入 main、组合后是否可部署。

## 3. 已确认 verdict

| Plan | Verdict | 后续动作 |
|---|---|---|
| 001 | `APPROVE` | 可复用，集成后重跑默认 pytest |
| 002 | `REVISE` | 重做 credential/env/transport 与 permission default-deny |
| 003 | `SUPERSEDED` | 使用 003a/003b 分项 |
| 003a | `APPROVE` | 可复用日志/退避，集成后回归 |
| 003b | `REVISE` | 等修正 identity migration 后再移除显式 seq |
| 004 | `REVISE` | 重做 legacy adoption、sequence、startup schema authority |
| 005 | `REVISE` | 以 query-count + shape tests 修完 reply-count N+1 |
| 006 | `APPROVE WITH DEPENDENCY` | maintainer 批准三主题 truth 后再改 docs |
| 007 | `NOT IMPLEMENTED` | 链接 observer task；默认延期，不碰用户 WIP |
| 008 | `NOT IMPLEMENTED` | 链接 capability matrix；另立 feature program |
| 009 | `REVISE` | 用真实 PG tombstone 语义重做 Task/File delete |
| 010 | `NOT IMPLEMENTED` | 记录 `/daemon` 产品决策后另做 route/nav |
| 011 | `NOT IMPLEMENTED` | 记录 Remotion 处置；未授权不清理 WIP |
| 012 | `REVISE` | canonical UUID actor normalization + alias matrix |
| 013 | `REVISE` | 数据库保证并发 bootstrap owner 唯一 |
| 014 | `REVISE` | privileged builtin + legacy classification + tenant slug |
| 015 | `APPROVE WITH DEPENDENCY` | 保留 app cap，补 ingress/disk/cleanup envelope |
| 016 | `APPROVE WITH DEPENDENCY` | 补 pool/listener 恢复与部署连接预算 |
| 017 | `REVISE` | public/agent stream 均不得持有 request DB session |
| 018 | `REVISE` | total-order cursor、filter-before-limit、frontend consumption |
| 019 | `REVISE` | 先建立真实绿 Ruff/build baseline 再启用 CI |
| 020 | `APPROVE` | 可复用 Bun/lockfile/dead-dep 清理 |
| 021 | `APPROVE WITH DEPENDENCY` | 复用前补 component/build/`./twd` 证据 |
| 022 | `APPROVE WITH DEPENDENCY` | 依赖 017；收敛为 one SSE owner + targeted refresh |
| 023 | `REVISE` | 修复登录/session/server fixture 后才可称 e2e |

## 4. 两个不可争辩的真实 PostgreSQL RED

### 4.1 Historical seq migration

一次性 PostgreSQL 16 已证明：baseline 表插入 `seq=1,2,3`，执行 advisor 0002 identity
迁移后，identity sequence 仍从 1 开始；第一条省略 seq 的 INSERT 报
`messages_seq_key`，`Key (seq)=(1) already exists`。

永久回归必须覆盖：

- fresh DB；
- baseline-version DB；
- legacy pre-Alembic fingerprint → stamp baseline only → upgrade head；
- 历史显式 seq；
- 过渡窗口旧显式 writer + 新隐式 writer；
- 自动 next value 永远大于所有 committed explicit/historical value；
- FastAPI startup 不运行 create-all/DDL。

禁止使用 `Base.metadata.create_all` 建空表测试来声称迁移正确，也禁止推荐 `stamp head`。

### 4.2 Task DELETE foreign keys

真实 PostgreSQL 按 advisor 路由顺序执行 `DELETE tasks` 后再插入带旧 task_id 的 activity/event，
分别命中 `activity_logs_task_id_fkey` 和 `event_records_task_id_fkey`。事务回滚，Task 仍存在。

终端顺序：

```text
capture primitive tombstone
→ delete dependencies/entity
→ ActivityLog(task_id=NULL, details=tombstone)
→ EventRecord(task_id=NULL, payload.taskId=old UUID, type=task.deleted)
→ commit
→ publish
```

永久测试必须是真实 PostgreSQL route/integration test，并证明 daemon 不把 `task.deleted` 当成
runtime/model work。

## 5. 其余必须重做的终端合同

### Auth/tenancy

- backend/frontend/compose/CI/proxy/docs 使用一个 coherent public-key env contract；
- production missing/known default fail closed；
- credential 不进入 query URL；
- missing permissions 明确 default-deny；
- omitted/display/`@handle`/UUID 归一到 canonical actor UUID 后做 self-only authorization；
- two independent transactions 证明最多一个 bootstrap owner；
- global builtin 仅 system/privileged，tenant template 必有 server_id；
- global builtin slug partial unique + `(server_id, slug)` tenant unique；
- legacy NULL 行显式分类，不能静默隐藏；cross-tenant ID 不泄露存在性。

### Runtime/querying

- 50/100-row request 的 whole-request query count 为有名的常数预算；response shape 不变；
- prefetch 使用显式 `_UNSET` 类 sentinel，不能把 `None` 混作“没提供”；
- upload 区分 ingress、Starlette spool、app memory、temporary disk、durable file，并测试失败清理；
- NOTIFY publisher/listener 有 owner、reconnect、generation、bounded shutdown 和 worker connection budget；
- public/agent SSE setup 只留下 frozen primitive claims，不捕获 session/ORM；
- server-wide task/thread 使用 total-order cursor，SQL filter before limit；
- frontend 消费 `nextCursor` 或显示诚实 load-more；
- 一个 account/server browser scope 只开一个 physical SSE connection。

### Delivery/UI

- Ruff baseline 与 production build env 在干净 checkout 上真实绿；
- Bun lockfile/packageManager/README/CI 一致；
- automated flow 创建真实 account/server/membership/session/active-server context；
- 不硬编码 key，不用 query credential；
- Task/File deletion 具有本地化确认、auth、错误回滚和 accessibility；
- repository UI acceptance 使用 `./twd`，不得用 Playwright 替代；
- worktree CWD/branch/URL/ports/DB 与 `smallkhoj-trace` markers 必须记录。

## 6. Advisor 分支使用规则

所有 advisor branch 都是 untrusted patch source：

1. 对照当前 child requirement；
2. 在 remediation base 先重现 intended RED；
3. 检查 advisor test 是否用 fake session、空表、源码字符串或把 bug 固化为预期；
4. 只 cherry-pick 自包含且语义正确的 commit，或最小重实现；
5. 在集成 candidate 上跑 child focused + full gates。

已知组合冲突：

- 005/018 在 `agent_api.py:list_threads`：必须同时满足 query budget、SQL filter-before-limit、
  stable traversal，不能用 ours/theirs；
- 012/014 在 task/template actor/server scope：必须合并两组终端 kwargs/authorization 语义；
- 017/022：SSE DB lifetime、one browser transport 与 targeted invalidation 必须共同验证；
- 019/023/002：CI build/e2e env 必须消费 auth child 的最终 credential contract；
- router architecture extraction 必须在上述行为冻结后进行。

旧 `/private/tmp/merge-audit-r3` 只合入部分分支并停在冲突中，不能作为完整组合证据。

## 7. Remediation 任务树

父任务：

```text
.trellis/tasks/07-22-codebase-audit-remediation/
```

子任务及依赖顺序：

```text
07-22-audit-remediation-schema-integrity
  -> 07-22-audit-remediation-auth-tenancy
  -> 07-22-audit-remediation-runtime-querying
  -> 07-22-audit-remediation-delivery-ui
  -> 07-22-audit-remediation-architecture-debt
product-directions（仅批准/延期/链接后执行对应范围）
  -> 07-22-audit-remediation-integration-release
```

每个 child 已有 `prd.md`、`design.md`、`implement.md`。执行前必须激活正确 child，读取相应
Trellis specs，并在 sibling worktree 中实施。

## 8. 工作区与安全边界

- 非 trivial 修改使用 sibling worktree 与 `feat/*` 分支；
- 创建前 `main` 必须与 `origin/main` ahead=0/behind=0，`docs/` clean；
- 保留 `MEMORY.md`、`session-observer/`、Remotion、capability matrix、harness diagnostics、
  runtime spec 等用户 WIP；不得 stage/move/delete；
- 不使用共享/live database 做迁移、并发或破坏性测试；
- repo UI 使用 `./twd` wrapper，不直接调用 `twd.py`，不以 Playwright 替代；
- backend/frontend/daemon 使用 worktree-specific ports，先证明 CWD/branch/URL；
- 所有 shell 命令使用项目 `rtk` 前缀。

## 9. 集成完成门禁

同一个 candidate SHA 必须完成：

1. `git diff --check`、task/spec/generated/schema validation；
2. fresh/versioned/legacy PostgreSQL migration matrix；
3. historical seq、owner race、template scope、delete transaction；
4. complete backend pytest + Ruff；
5. frontend frozen Bun install + tests + lint + typecheck + production build；
6. authenticated automated management flow；
7. NOTIFY/SSE/resource/query/pagination connection and traversal evidence；
8. worktree runtime health/trace；
9. `./twd` authenticated deletion、>50 pagination、loading/error、targeted refresh、chat、theme/route；
10. original 001–023/003a/003b 与 non-plan findings 逐项重新 verdict/disposition；
11. 中文报告、技术报告、plan index、DESIGN、migration/auth/testing/spec truth 同步；
12. Trellis quality gate、peer review、正常 PR + squash merge。

任一 mandatory gate 失败或 flaky 都是未完成。candidate green 只能写“candidate verified”；PR
合入后才能写“merged”；实际部署并观察健康窗口后才能写“released/healthy”。

## 10. 建议的起点

从 schema-integrity child 开始：

1. 建立 diagnostic capsule；
2. 将已取得的一次性 PostgreSQL seq/delete RED 变成永久测试；
3. 修 identity alignment、legacy baseline adoption、startup DDL authority 和 tombstone semantics；
4. 通过完整 migration/backend gates；
5. 再进入 auth-tenancy。

不要从 advisor 分支批量 merge 开始，也不要先做大路由重构。真实数据不变量、安全边界和长连接
资源合同必须先稳定，架构提取排在 characterization 之后。

## 11. 关联文件

- 人类可读报告：`docs/2026-07-代码审计报告.md`
- 原技术审计：`.trellis/tasks/07-20-07-19-codebase-audit/research/2026-07-19-codebase-audit.md`
- 独立复核：`.trellis/tasks/07-20-07-19-codebase-audit/research/2026-07-22-independent-verification.md`
- 原 plan index：`.trellis/tasks/07-20-07-19-codebase-audit/plans/README.md`
- 修复父任务：`.trellis/tasks/07-22-codebase-audit-remediation/`
- 集成 release：`.trellis/tasks/07-22-audit-remediation-integration-release/`

交接原则只有一句：**不要相信旧的 APPROVE 声明；让同一个集成候选上的直接证据说话。**
