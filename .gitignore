# Python
__pycache__/
*.pyc
*.pyo
.venv/
.env
.env.*
!.env.example
!**/.env.example

# Node
node_modules/
.next/
agent/daemon/aaa-daemon/dist/

# IDE
.idea/
.DS_Store
**/.DS_Store
zy-think/.obsidian/

# Build caches
*.tsbuildinfo
toremove/
release-artifacts/
/aura-build-runtime/
/codex-acp.exe
/node.zip

# Trellis evidence screenshots are local validation artifacts; keep text notes, ignore image blobs.
.trellis/tasks/**/evidence/*.png
.trellis/tasks/**/evidence/*.jpg
.trellis/tasks/**/evidence/*.jpeg
.trellis/tasks/**/evidence/*.gif
.trellis/tasks/**/evidence/*.webp

# Dev server runtime
.dev-pids/
.dev-logs/
/frontend/.playwright-cli/
/frontend/.runtime/
/remotion/
smallkhoj-daemon.pid
.slock/
.slock-runtimes/
agent/daemon/aaa-daemon/.slock/
agent/daemon/aaa-daemon/aaa-daemon.pid
backend/.data/

# CodeGraph knowledge-graph index (generated, ~8MB SQLite + WAL; per-machine)
.codegraph/

# Graphify knowledge-graph output (generated, AST-only; per-machine)
graphify-out/

# Locally installed Claude skill bundles
.claude/skills/gh-fix-ci/
.claude/skills/playwright/
.claude/skills/security-best-practices/
.claude/skills/security-ownership-map/
.claude/skills/security-threat-model/
.claude/skills/sentry/

# Keep VS Code and agent configs
!.vscode/
!.codex/
!.cursor/
!.claude/
!.trellis/
!.agents/

# MCP config (may contain secrets like API keys)
.mcp.json

# Local private keys and cloud credentials
*.pem
*.key
*-ssh-key
*-ssh-key.*
tengxun-ssh-key*

# Local agent runtime/config state
.agent/
.cat-cafe/
.gemini/
.kimi/
BACKLOG.md
# Runtime agent may write a workspace MEMORY.md stub at repo root; the real
# runtime memory lives under .slock/. Keep the root clean.
/MEMORY.md
.claude/settings.json
.claude/settings.local.json
.codex/config.toml
.codex/hooks.json
.codex/skills/
.runtime/integration-gate/
# >>> Comet managed project state >>>
!/.comet/
/.comet/*
!/.comet/config.yaml
# <<< Comet managed project state <<<

# ZCode 本地产物（会话计划/平台命令不入库；rules 与 skills 中 comet 相关已跟踪保留）
.zcode/commands/
.zcode/plans/
