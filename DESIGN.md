# SmallKhoj Design System

本文件是 SmallKhoj 前端的视觉锚点。任何配色/质感改动以此为准；impeccable 与落地代码都参照本文件。

来源：用户提供的「阳光穿透水体的中海蓝 + 沙滩」参考图 + 设计对齐会（2026-06-24）。

## 设计意图（一句话）

明亮、透亮的中海蓝，像阳光穿透水体后剩下的短波长蓝光；用暖沙色平衡冷蓝；界面「会呼吸」但不喧宾夺主。是一个让人想长时间待着的工具，不是宣传片。

> **方向反转声明**：本主题**取代** `06-22-frontend-visual-redesign-theme` 的 dark-default + 蓝紫渐变决策。新方向是 **light-first + 单色中海蓝 + 有光感**。当前 `.dark` 默认将被改为 light 默认，深色保留为可切换的第二主题。

## 色彩与质感旋钮（已对齐）

| 旋钮 | 取值 | 说明 |
|---|---|---|
| 明暗基调 | **浅色为主** | light-first；深色可切换 |
| 蓝浓度 | **中海蓝（中）** | hue 205–220，实色，只用在强调位 |
| 光感强度 | **有光感（中）** | 背景微渐变 + 关键元素柔光晕；无玻璃拟态、无渐变文字 |

## 色板（OKLCH）

> 色相全部落在短波长蓝（hue 200–225），这是「阳光穿水后剩下的蓝」，区别于旧的紫蓝（hue 260+）。暖色只用于表面平衡。

| Token | 值 | 角色 |
|---|---|---|
| `--background` | `oklch(0.985 0.008 220)` | 底/天空：近白带极淡冷蓝调 |
| `--card` | `oklch(0.975 0.012 85)` | 表面/沙滩：暖沙白，平衡冷蓝 |
| `--foreground` | `oklch(0.28 0.04 225)` | 墨色：深蓝灰文字，非纯黑 |
| `--primary` | `oklch(0.62 0.13 215)` | 中海蓝：品牌/主按钮/链接 |
| `--accent` | `oklch(0.50 0.11 218)` | 深海蓝：强调/层次 |
| `--ring` / focus | `oklch(0.72 0.10 200)` | 水面高光：偏青，focus + 光晕 |
| `--muted-foreground` | `oklch(0.55 0.03 225)` | 次要文字：蓝灰，对比度仍 ≥4.5:1 |
| `--border` | `oklch(0.91 0.015 220)` | 描边：淡蓝灰 |

## 光感规则（有光感·中）

- **背景**：极淡自上而下明度渐变（顶 L0.985 → 底 L0.96，轻微蓝移），模拟水面亮→深处略沉。仅 body 层，不进入卡片内部。
- **主按钮 / active**：实色中海蓝（`--primary`），hover 加柔和外光晕（`box-shadow: 0 0 0 4px oklch(0.62 0.13 215 / 0.18)`，blur ≤8px），非 neon。
- **focus ring**：水面高光色 + 轻发光。
- **禁用**：玻璃拟态（glassmorphism）、`background-clip:text` 渐变文字、大面积模糊光斑、repeating 条纹。

## 质感边界

- 圆角 `--radius`: `0.875rem`（保持现状）。
- 正文对比度 ≥ 4.5:1；placeholder 同样 4.5:1，不用灰底淡字。
- 正文行宽 65–75ch；标题 `text-wrap: balance`。

## 深色主题（第二主题，可切换，非默认）

保留并适配新色系：深底仍为蓝灰（非纯黑），品牌色仍用中海蓝（提亮一档以适配暗底）。不使用旧的紫蓝 hue 260。具体值在落地时补。

## 待办（落地阶段）

- [ ] 将 light 设为默认（`layout.tsx` 默认不挂 `.dark`）
- [ ] 替换 `globals.css` 的 `:root` 与 `.dark` 色板为上表
- [ ] 移除/重定义旧渐变变量（`--gradient-primary` 蓝紫等），改为单色中海蓝 + 光晕方案
- [ ] 主按钮 variant 改为实色 + hover 光晕
- [ ] 用 `./twd` 做真实截图校验对比度与光感
